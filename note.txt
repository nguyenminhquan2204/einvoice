nx lint invoice
nx test invoice
nx run invoice:serve
nx test product --coverage
nx e2e einvoice-e2e
nx reset & nx run-many -t serve --projects=bff,invoice

-------Library-------------
01. pnpm add -D husky
02. pnpm add -D lint-staged
03. pnpm add -D @commitlint/cli @commitlint/config-conventional
04. pnpm add @nestjs/config
05. pnpm add class-validator class-transformer
06. pnpm add @nestjs/swagger
07. pnpm add express
08. pnpm add -D @types/express
09. pnpm add uuid
10. pnpm add @nestjs/microservices
11. pnpm add @nestjs/throttler
12. pnpm add @nest-lab/throttler-storage-redis
13. pnpm add @grpc/grpc-js @grpc/proto-loader
14. pnpm add puppeteer
15. pnpm add ejs
16. pnpm add -D @types/ejs
17. npx puppeteer browsers install chrome
18. pnpm add cloudinary
19. pnpm add streamifier
20. pnpm add -D @types/streamifier
21. pnpm add stripe
22. pnpm add kafkajs
23. pnpm add nodemailer
24. pnpm add -D @types/nodemailer
25. pnpm add -D @testcontainers/postgresql
26. pnpm add @nestjs/terminus
27. pnpm add nestjs-pino pino pino-http pino-pretty pino-loki
28. pnpm add @willsoto/nestjs-prometheus prom-client

---------------------------

------Husky, Lint-Staged, Commit Lint---------------
pnpm exec husky init
pnpx lint-staged
pnpm add -D @commitlint/cli @commitlint/config-conventional
echo "export default { extends: ['@commitlint/config-conventional'] };" > commitlint.config.js
echo "npx --no -- commitlint --edit \$1" > .husky/commit-msg

----Port-----------
lsof -nP -iTCP:2204 -sTCP:LISTEN
kill -9 $(lsof -t -iTCP:2204 -sTCP:LISTEN)


-----Install Stripe-------
1. brew install stripe/stripe-cli/stripe
2. stripe login
3. stripe listen --forward-to localhost:2204/api/v1/webhook/stripe
4. stripe trigger

----Config kafka----------
1. kafka-topics.sh --create --topic demo-topic-1 --bootstrap-server localhost:9092
2. kafka-topics.sh --list --bootstrap-server localhost:9092

------Draft----------------
1. "dev-lite": "pnpm dev --projects=bff,user-access,authorizer,invoice,pdf-generator,media,mail"
2. prometheus
3. Open Telemetry
4. Grafana Tempo
5. pnpm docker:start:local pdf-generator

